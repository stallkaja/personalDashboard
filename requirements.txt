# Backend Python dependencies for listener.py (personal dashboard).
# Install into the SAME interpreter the Dashboard-Backend service will run:
#   python -m pip install -r requirements.txt
# Pinned to the versions running on the source machine (Python 3.14, MySQL 9.7).

Flask==3.1.3
Flask-Cors==6.0.5
Flask-SocketIO==5.6.1
Flask-JWT-Extended==4.7.4
Werkzeug==3.1.8
PyJWT==2.13.0
pywebpush==2.3.0
py-vapid==1.9.4
bcrypt==5.0.0
mysql-connector-python==9.7.0
requests==2.34.2
cryptography==49.0.0
tzdata==2026.2

# Transitive (pinned for reproducibility; pip would pull these anyway)
python-socketio==5.16.3
python-engineio==4.13.2
simple-websocket==1.1.0
bidict==0.23.1
